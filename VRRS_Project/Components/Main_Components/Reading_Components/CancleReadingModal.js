import { View, Text } from "react-native";
import { Modal } from "react-native";

export default function CancleReadingModal({ visible }) {
  return (
    <Modal visible={visible}>
      <View></View>
    </Modal>
  );
}
