import { View, Text } from "react-native";
import { StyleSheet } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ReadingIngridientScreen({ navigation }) {
  return (
    <SafeAreaView>
      <Text>원재료명</Text>
    </SafeAreaView>
  );
}
